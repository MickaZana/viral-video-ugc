{
  "name": "viral-video-ugc",
  "private": true,
  "version": "0.1.0",
  "description": "Claude-powered, cloud-orchestrated viral video discovery/repurposing pipeline",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "cli": "pnpm --filter @vvugc/orchestrator cli"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0",
    "vitest": "^2.1.4"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  }
}
